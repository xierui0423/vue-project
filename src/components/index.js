import footer from './footer/footer.vue';
import header from './header/header.vue';
import todos from './todos/todos.vue';

// Exports the global components so they don't need to be included in every page explicitly.
export default {footer, header, todos};